apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: restward-api-ingress
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - REPLACE_HOST
      secretName: restward-api-tls
  rules:
    - host: REPLACE_HOST
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: restward-api
                port:
                  number: 80
