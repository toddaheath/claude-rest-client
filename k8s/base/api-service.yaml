apiVersion: v1
kind: Service
metadata:
  name: restward-api
  labels:
    app: restward-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 5000
      protocol: TCP
  selector:
    app: restward-api
